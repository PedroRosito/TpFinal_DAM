import { ChangeAperturaPipe } from './change-apertura.pipe';

describe('ChangeAperturaPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeAperturaPipe();
    expect(pipe).toBeTruthy();
  });
});
